<% @posts.each do |post| %>
  <div class="col-md-4 col-md-offset-4 col-xs-4 col-xs-offset-4 col-sm-4 col-sm-offset-4 col-lg-4 col-lg-offset-4">
      <h2>
        <%= link_to post.item, post %><br>
        <small class=""><%= post.country%></small>
        <small class="author">Submitted <%= time_ago_in_words(post.created_at) %> by <%= post.user.name %></small>
      </h2>

      <div class="btn-group">
        <%= link_to like_post_path(post), method: :put, class: "btn btn-default btn-sm" do %>
          <span class="glyphicon glyphicon-chevron-up"></span>
          Upvote
          <%= post.get_upvotes.size %>
        <% end %>
        <%= link_to dislike_post_path(post), method: :put, class: "btn btn-default btn-sm" do %>
          <span class="glyphicon glyphicon-chevron-down">
          Downvote
          <%= post.get_downvotes.size %>
        <% end %>
      </div>
  </div>
<% end %>
